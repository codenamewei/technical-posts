FROM python:3.11-slim

COPY . /app/

WORKDIR /app

RUN pip install -r requirements.txt && cp $(which uvicorn) /app
ENV PYTHONPATH=/usr/local/lib/python3.11/site-packages

CMD ["./uvicorn", "backend:app", "--host", "0.0.0.0", "--port", "8000"]